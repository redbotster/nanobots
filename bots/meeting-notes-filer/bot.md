# meeting-notes-filer

Turn a transcript into notes, decisions, and action items; file them to Drive.

`openclaw` harness: `transcript` is read as real text (via the interpreter's file-input-to-text resolution), summarised into notes/decisions/action items in one `ai.generate` call, rendered to an HTML file, and saved to Drive.
